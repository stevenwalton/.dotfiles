/*
 * User configuration file for FireFox.
 *      Alternative to `about:config`
 * Docs: https://kb.mozillazine.org/User.js_file
 *
 * Changes made in `about:config` will not be persistent, and be overwritten by
 * what's contained herein.
 * Preferences placed here are copied into `prefs.js`, in the same profile
 * folder. These preferences overwrite 
 *
 * Place in:
 *  Linux: 
 *      ${HOME%/}/.mozilla/firefox/<profile folder>
 *  OSX (either): 
 *      ${HOME%/}/Library/Application Support/Firefox/Profiles/<profile folder>
        ${HOME%/}/Library/Mozilla/Firefox/Profiles/<profile folder>
*/
